import time
import multiprocessing,Queue
import traceback
import globle
import re as RE
import threading
import xml.etree.ElementTree as ET
device_que=multiprocessing.Queue()
# print(__name__)
# a=[1,2]
# import threadpool
# def fun2(q1):
#     time.sleep(30)
#     while not q1.empty():
#         print(q1.get())
#
#        #c[0] +=1
# def fun1(q1):
#     return 1
#
# def callback_func(item):
#     c = globle.get_list()
#     c.append(item)
# class a():
#     def __init__(self,name,age):
#         self.name=name
#         self.age=age
#
# class No1ResultsPending(Exception):
#     """All work requests have been processed."""
#     pass
# def re():
#     return 0
#
# def get_namespace(element):
#     m = RE.match('\{.*\}', element.tag)
#     return m.group(0) if m else ''
if __name__=="__main__":
    # file=open("C:/Users/Administrator/Desktop/lsd/test/new.xml")
    # text=file.read()
    # file.close()
    # root_namespace = {"A1":"urn:psialliance-org","B":"urn:psialliance-org"}
    # ET.register_namespace("A", "urn:psialliance-org")
    # node=ET.fromstring(text)
    # name=get_namespace(node)
    # s="Network Rideo Recorder CCD"
    # s1="Network Rideo Recorder"
    # if s1 in s:
    #     print(11111111)
    # print("{0}VideoInputChannel".format(name))
    # for item in node.iter("{0}VideoInputChannel".format(name)):
    #     print(get_namespace(item))
    #     node_id=item.find("{0}id".format(name))
    #     print(node_id.text)
   # print(list1)
    dic={}
    dic.update({"a":123})
    if not dic:
        print(2)
    


